<%= link_to "" , 
    entry_path(current_user.ents.find_by(:event_id => event.id) , 
      event_id: event.id , type: "Ent") ,
    :class => "want-#{event.id} btn-want btn btn-danger btn-sm wanted",
    :remote => true,
    :method => :delete %>