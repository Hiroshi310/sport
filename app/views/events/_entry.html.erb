<%= link_to "" , entrys_path(event_id: event.id , type: "Entry") ,
                      :class => "entry-#{event.id}  btn btn-warning btn-sm entry",
                      :remote => true,
                      :method => :post %>