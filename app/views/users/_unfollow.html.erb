<%= form_for(current_user.following_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= button_tag( class: "btn btn-sm") do %>
      <%= content_tag :span, "フォロー解除", class: "glyphicon glyphicon-remove" %>
<% end %>
<% end %>