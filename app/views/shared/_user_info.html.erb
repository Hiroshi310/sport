<div class="user-profile">
  <div class="icon">
    <%= link_to gravatar_for(current_user, size: 50), current_user %>
  </div>
  <div class="name">
    <%= current_user.name %>

  <span><%= link_to "view my profile", current_user %></span>
  <span><%= pluralize(current_user.events.count, "") %>の企画</span>
    </div>
